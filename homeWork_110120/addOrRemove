Test Case ID: 1
Run Result ID: 1
Login


----------------
Summary:
Test to see if the Add Element when clicked adds a "Delete" button and if the Remove function works if a user clicks the "Delete" button that it would delete the "Delete" button.

Setup:
https://the-internet.herokuapp.com/add_remove_elements

Steps:
1. Click the "Add Element" button.
2. Click the "Delete" button.

Result:
A new "Delete" button shows up as the user clicks the "Add Element" button also removes the "Delete" button as the user clicks it.
